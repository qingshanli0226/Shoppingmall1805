<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tl="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.shopmall.bawei.common.MyToolBar
       style="@style/MyToolBar"
        android:id="@+id/toolbar"
        app:left_show="false"
        app:title_show="false"
        app:right_src="@mipmap/menu"
        app:right_text="消息"
        app:right_text_color="@android:color/white"
        android:background="@android:color/holo_red_light"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:padding="5dp">

        <com.flyco.tablayout.SegmentTabLayout
            android:id="@+id/tl_1"
            android:layout_width="150dp"
            android:layout_height="35dp"
            android:layout_centerInParent="true"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="10dp"
            tl:tl_bar_color="#ffffff"
            tl:tl_indicator_color="#ff0000"
            tl:tl_indicator_corner_radius="20dp"
            tl:tl_tab_padding="23dp" />

        <ImageView
            android:id="@+id/iv_type_search"
            style="@style/ImageViewStyle"
            android:layout_alignParentRight="true"
            android:layout_marginRight="@dimen/img_right"
            android:layout_marginTop="@dimen/img_top"
            android:background="@drawable/icon_search_white" />


    </RelativeLayout>

    <FrameLayout
        android:id="@+id/fl_type"
        style="@style/FrameStlys"/>

</LinearLayout>