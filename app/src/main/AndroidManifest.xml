<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elevenmonthshoppingproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:name=".MasterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".address.view.AddressMessageActivity" />
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/AppTheme.FullSrcreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".product.view.ProductDetailActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.FullSrcreen" />
        <activity
            android:name="com.example.shopercar.view.ShoppingPageActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".message.MessageListActivity"
            android:theme="@style/AppTheme.FullSrcreen" />

        <receiver android:name=".home.view.HomeFragment$MyBroadcaseReceiver" />
    </application>

</manifest>